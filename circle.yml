machine:
  services:
    - docker
  node:
    version: 7.4.0

dependencies:
  override:
    - echo "No deps!"

test:
  override:
    - ./ci/build.sh
    - ./ci/test.sh

deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    owner: reactioncommerce
    commands:
      - ./ci/deploy.sh
